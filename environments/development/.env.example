# Copy to .env in this directory and adjust. Used by docker compose for Ghost + MySQL.

# Browser-facing site URL (must match how you open the stack, e.g. Caddy on port 80).
PUBLIC_URL=http://localhost

# MySQL root password for the Ghost database (Ghost uses the same value).
GHOST_DB_PASSWORD=change_me
